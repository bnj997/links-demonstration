import styled from "styled-components";

export const SubtitleWrapper = styled.h4`
  font-size: ${(props) => props.theme.font.sizes.subtitleSize};
  font-family: ${(props) => props.theme.font.family.primary};
  font-weight: 500;
`;
